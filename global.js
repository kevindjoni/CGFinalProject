var cupsTotal = 5; // can be changed later // number of cups
var cupDist = 25 // distance between cups.
var shuffleNumber = 10; // number of times cup shuffle
var cupWithBall = Math.floor(Math.random() * cupsTotal);
